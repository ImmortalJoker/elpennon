Code style is lightweight and component-oriented. Astro components keep small frontmatter blocks and inline client scripts where needed. Tailwind utility classes drive most styling, with brand tokens defined in src/styles/global.css. Typography uses Playfair Display for headings and Inter/system for body text. Keep changes consistent with the existing warm gold/dark palette and avoid introducing unrelated frameworks or heavy abstractions for simple UI behavior.